# HypeTuneCluster

HypeTuneCluster is a lightweight toolkit for running hyperparameter tuning workflows
on HPC clusters and local machines. It handles trial config generation, job
submission (Slurm or local), monitoring, and metric collection so Optuna-style
search loops can run with minimal orchestration code.
